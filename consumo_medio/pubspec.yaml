name: my_mobx_project  # Renomeie para algo diferente de 'mobx'
description: "A new Flutter project using MobX for state management."
publish_to: 'none'  # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.7.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  mobx: ^2.1.1

dev_dependencies:
  build_runner: ^2.4.6
  mobx_codegen: ^2.1.1
  
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
